---
description: "Run the starter-pack quality pipeline consolidado: lint + typecheck + tests + coverage + security scan. Use pre-commit como gate, en CI local, o antes de pedir review de PR."
agent: build
---
Run the quality pipeline: lint, typecheck, tests, coverage check, security scan. Report results and block if any check fails.
