---
description: "List registered projects con instinct counts (project + global). Use when quieras ver el estado multi-proyecto y cuantos instincts tiene cada uno antes de sync o migrate."
agent: build
---

# Projects Command

Show opencode-native project registry and stats: $ARGUMENTS

## Your Task

```bash
node .opencode/bin/instinct.js projects
```
