---
description: "Fix Flutter/Dart build errors, pub dependency conflicts, and build_runner issues with minimal changes. Use when `flutter build` o `dart analyze` falla."
agent: dart-build-resolver
---
Fix Flutter build errors. Run flutter build, parse errors, fix one at a time, re-run. Run flutter analyze after fixes.
