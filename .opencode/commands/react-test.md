---
description: "Run React component tests with Vitest, Jest, or React Testing Library. Use when quieras validar React sin TDD estricto — corre y reporta failures con coverage."
agent: react-build-resolver
---
Run React tests. Detect framework (Vitest/Jest), run tests with coverage. Fix failures incrementally.
