<!-- TEMPLATE:PROJECT-MD v2.1 -->
<!--
  Source of truth: project files + git + docs/ inventory + pack catalog.
  Manual sections (preserved across refresh): Non-Negotiables, Architecture Notes, Open Questions, Glossary.
  Auto-managed (append-only): Recent Activity.
  Optional sections (rendered only if detected): Data Model, API Surface, Dependencies, Container Build.

  Marker legend:
    <!-- auto:... -->         — what to detect (hint for the CLI implementer)
    <!-- manual -->            — preserved verbatim
    <!-- auto-managed -->      — append-only, idempotent
    <!-- auto+manual -->       — detected + user-overridable
    <!-- conditional:KEY -->   — section renders only if KEY is detected; CLI fills in values
    <!-- /conditional -->

  Generated by: `node .opencode/bin/project-init.js`
-->
# {NAME} — Project Context

> **Status**: {STATUS_EMOJI} {STATUS_LABEL} ({AGE_TEXT}) · **Init**: {INIT_DATE} · **Refresh**: {REFRESH_DATE} · **Stale after**: 3 days
> **Template**: `.opencode/templates/PROJECT.md.template` v2.1
> Manual sections (preserved): Non-Negotiables · Architecture Notes · Open Questions · Glossary
> Auto-managed (append-only): Recent Activity
> Init/refresh: `node .opencode/bin/project-init.js [--init|--refresh|--status]`

---

## Identity
<!-- auto: name from package.json/pubspec/pyproject/Cargo/go.mod, type inferred from stack, desc from README -->

- **Name**: {name}
- **Type**: {web-app | api-service | cli | library | monorepo | opencode-pack | ...}
- **Description**: {one-line from README or package.json}
- **Repo**: {git remote URL or "local only"}
- **Primary language**: {language}

## Stack
<!-- auto: detect from package.json, pubspec.yaml, pyproject.toml, Cargo.toml, go.mod, *.csproj, pom.xml -->

- **Language**: {lang}
- **Framework**: {framework + version}
- **Runtime / Build**: {node 20, python 3.12, rust stable, ...}
- **Package manager**: {npm | pnpm | yarn | poetry | cargo | go mod | pub | gradle}
- **Database**: {postgres | sqlite | mongodb | ... | none detected}
- **Deployment**: {vercel | aws | docker | fly.io | ... | not detected}

## Tooling
<!-- auto: detect from .eslintrc, .prettierrc, vitest.config, jest.config, pytest.ini, .github/workflows, Dockerfile, .env.example -->

- **Test runner**: {vitest | jest | pytest | go test | ...}
- **Coverage**: {c8 | codecov | ... | not detected}
- **Linter**: {eslint | biome | ruff | golangci-lint | ...}
- **Formatter**: {prettier | biome | black | rustfmt | gofmt}
- **CI**: {GitHub Actions (3 workflows) | GitLab CI | ...}
- **Container**: {Dockerfile | docker-compose | ...}
- **Env vars**: {12 detected — see .env.example}

## Build & Run
<!-- auto+manual: detect from package.json scripts, Makefile, justfile, Cargo.toml bins. Override below if needed. -->

- **Install**: `npm install` *(or pnpm/poetry/cargo — see Lockfile section)*
- **Dev**: `npm run dev` *(starts on :3000)*
- **Test**: `npm test` *(vitest --run)*
- **Lint**: `npm run lint` *(eslint .)*
- **Build**: `npm run build` *(tsc + vite build)*
- **Deploy**: `vercel --prod`

<!-- Override the detected commands above. Refresh keeps your edits. -->

## Conventions
<!-- auto+manual: detect from existing code (naming, imports, error style). Edit below to override. -->

- **Naming**: {kebab-case files, PascalCase classes, camelCase functions}
- **File structure**: {src/ for code, tests/ for tests, docs/ for docs}
- **Error handling**: {try/catch with custom Error classes | Result types | exceptions}
- **Commits**: {conventional commits | semantic release | free-form}
- **Branching**: {git-flow | trunk-based | GitHub flow | free-form}
- **PR review**: {required | optional | single-approver | CODEOWNERS}

<!-- Override auto-detected values below. Refresh keeps your edits. -->

## Entry Points
<!-- auto: from package.json main/bin, common patterns (src/index.ts, app.ts, cmd/main.go, src/main.rs) -->

- `src/index.ts` — main entry
- `bin/cli.js` — CLI binary

## Directory Layout
<!-- auto: top 2 levels of common dirs (src, lib, app, test, docs, scripts) with one-line purpose -->

- `src/` — application code
  - `src/api/` — HTTP routes
  - `src/services/` — business logic
  - `src/db/` — database access
- `tests/` — test suites
- `docs/` — project documentation (this file lives here)
- `scripts/` — dev scripts

## Domain Map
<!-- auto: detect from top-level dirs and their contents; one-line per module/area -->

- **{module}**: {one-line purpose}
- **{module}**: {one-line purpose}

<!-- conditional:data-model -->
## Data Model
<!-- auto: detect from prisma/schema.prisma, db/migrations, src/types/, models/, src/db/schema.ts -->

- **{Entity}**: {description, key fields}
- **{Entity}**: {description, key fields}
<!-- /conditional -->

<!-- conditional:api-surface -->
## API Surface
<!-- auto: detect from src/api/, src/routes/, exported functions in index.ts, bin/ CLI commands -->

### HTTP endpoints
- `GET /api/{resource}` — {description}
- `POST /api/{resource}` — {description}

### Exported functions / libraries
- `createUser(data)` — {description}
- `getUserById(id)` — {description}

### CLI commands
- `{tool} {command}` — {description}
<!-- /conditional -->

<!-- conditional:dependencies -->
## Dependencies
<!-- auto: top 10 from package.json/requirements.txt/Cargo.toml, with rationale from README or comments -->

- **{dep} {version}**: {why chosen}
- **{dep} {version}**: {why chosen}
<!-- /conditional -->

## Glossary
<!-- manual: domain-specific terms. Helps sub-agents disambiguate "user" vs "account" vs "tenant" without re-deriving. -->

- **{term}**: {definition in this project's context}
- **{term}**: {definition}

## Non-Negotiables
<!-- manual: things that MUST stay true. Preserved across refreshes. -->

- **{constraint}**: {e.g. License: MIT, no telemetry, no third-party analytics, must run on Node 20+}

## Architecture Notes
<!-- manual: ADRs, rationale, key decisions. Preserved across refreshes. -->

- **{decision}**: {rationale}

## Open Questions
<!-- manual: things to resolve later. Preserved across refreshes. -->

- [ ] {question}
- [ ] {question}

## Recent Activity
<!-- auto-managed: appended by `node .opencode/bin/project-init.js --append-event TYPE NAME`. Do not edit by hand. -->

<!-- Entry format:
- {ISO date} — [{type}]({file-or-url}): {title} {meta}
  Examples:
  - 2026-07-27 — [prd](docs/prds/2026-07-27_1800-user-auth.prd.md): "User auth" (12 acceptance criteria)
  - 2026-07-27 — [plan](docs/plans/2026-07-27_1830-user-auth.plan.md): 12/15 steps complete
  - 2026-07-27 — [audit](docs/audits/2026-07-27_1900-pack-1.1.audit.md): APPROVED for tag 1.1.0
  - 2026-07-27 — [commit](https://github.com/marchelero/open/commit/60b1d9a): "feat: pack 1.1 polish — AGENTS slim…"
-->
