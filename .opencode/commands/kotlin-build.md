---
description: "Fix Kotlin/Gradle build errors and dependency conflicts with minimal changes. Use when `./gradlew build` falla, hay errores de resolucion, o kapt/ksp se rompe."
agent: kotlin-build-resolver
---
Fix Kotlin/Gradle build errors. Run ./gradlew build, parse errors, fix one at a time.
