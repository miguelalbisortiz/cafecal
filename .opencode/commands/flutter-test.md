---
description: "Run Flutter widget and unit tests with `flutter test`. Use when quieres validar Flutter sin TDD estricto — corre y reporta failures con coverage."
agent: dart-build-resolver
---
Run flutter test. Report failures. If failures found, fix one at a time and re-run. Run with --coverage and report percentage.
