---
description: "Fix C++ build errors, linker issues, and template errors with minimal changes (no refactor). Use when cmake/make falla, hay linker errors, o template instantiation explota."
agent: cpp-build-resolver
---
Fix C++ build errors. Detect CMake/Make, run build, parse errors, fix one at a time.
